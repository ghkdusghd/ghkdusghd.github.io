---
title: "백준 2948번 2009년"
parent: 배열 활용
nav_order: -7

toc: true
toc_sticky: true

date: 2026-01-13
---

# <span style="color: #7153ED; font-weight: bold;">Q 2948 </span> 2009년

## 💡 문제 요약 및 분석

2009년 날짜가 주어졌을 때, 무슨 요일인지 출력하자. 참고로, 2009년 1월 1일은 목요일이며, 윤달은 없다.

## 💡 알고리즘 설계

1. 배열을 두 개 선언한다. ```int[] months = {0, 31, 28, 31, 30, ..., 31};``` ```String[] weeks = {"Thursday", "Friday", ..., "Wednesday"};```

2. M까지 months 배열의 일자를 더하고, D 를 더한다. ```for(int i = 0; i < M; i++) { 연산 }```

3. 더한 값을 7로 나누고, 그 나머지가 n 이라면, weeks 배열에서 n-1번째 요소를 출력한다.

## 💡 코드

``` java

public class Main {

    public static void main(String[] args) throws Exception {
        int[] months = {0, 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};
        String[] weeks = {"Thursday", "Friday", "Saturday", "Sunday", "Monday", "Tuesday", "Wednesday"};

        int D = read();
        int M = read();

        int count = 0;
        for (int i = 0; i < M; i++) {
            count += months[i];
        }
        count += D;
        int n = count % 7 - 1;

        // ArrayIndexOutOFBounds 에 대한 예외 처리
        if (n == -1) {
            System.out.println("Wednesday");
            return;
        }
        
        System.out.println(weeks[n]);
    }

    static int read() throws Exception {
        int c = 0, n = 0;
        while ((c = System.in.read()) > 47) n = (n << 3) + (n << 1) + (c & 15);
        return n;
    }

}
```

<img src="/assets/images/pages/algorithms/array/스크린샷 2026-01-13 오후 7.55.38.png">

## 💡 틀린 부분 분석

위와 같은 방식이라면 "수요일"의 경우, n = -1 이 되어 버려서 weeks[n] 에서 런타임 에러가 발생해서 코드를 살짝 수정했다. 다만, 간단한 연산인데 코드가 너무 길어져서 좀더 단순하게 수정해 보았다.

``` java

public class Main {

    public static void main(String[] args) throws Exception {
        int[] months = {0, 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};
        String[] weeks = {"Wednesday","Thursday","Friday","Saturday","Sunday","Monday","Tuesday"};

        int D = read();
        int M = read();

        for (int i = 0; i < M; i++) {
            D += months[i];
        }

        System.out.println(weeks[D%7]);
    }

    static int read() throws Exception {
        int c = 0, n = 0;
        while ((c = System.in.read()) > 47) n = (n << 3) + (n << 1) + (c & 15);
        return n;
    }

}
```

- count 변수를 굳이 사용할 필요가 없었다. 입력 받은 날짜 D에 이전 달의 날짜들을 더하면 됨.

- ```D % 7 - 1``` 로 -1 연산을 할 필요 없이, weeks 배열 자체에서 순서를 조정하면 된다. 1월 1일이 목요일이었기 때문에 첫 요소를 "Thursday" 로 시작했지만, 자체적으로 -1 하여 "Wednesday" 를 첫 요소로 만들면 복잡한 연산이 필요 없다.

<!-- ## 💡 알고리즘 재설계 및 정답 코드 -->

## 💡 시간 복잡도, 공간 복잡도

| 시간 복잡도 | 공간 복잡도 |
|---|---|
| O(1) | O(1) |

<!-- ## 💡 다른 풀이 -->

<!-- ## 💡 느낀점 및 기억할 정보 -->

